<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">

    <uses-permission android:name="android.permission.INTERNET" />

    <application
        android:name="androidx.multidex.MultiDexApplication"
        android:allowBackup="true"
        android:dataExtractionRules="@xml/data_extraction_rules"
        android:fullBackupContent="@xml/backup_rules"
        android:icon="@drawable/sdmit"
        android:label="@string/app_name"
        android:roundIcon="@drawable/sdmit"
        android:supportsRtl="true"
        android:theme="@style/Theme.TheWisher"
        tools:targetApi="31">
        <activity
            android:name=".Enter_dets"
            android:exported="false" />
        <activity
            android:name=".Des"
            android:exported="false" />
        <activity
            android:name=".Select_elems"
            android:exported="false" />
        <activity
            android:name=".SelectElemsActivity"
            android:exported="false"
            android:label="@string/title_activity_select_elems"
            android:theme="@style/Theme.TheWisher" />
        <activity
            android:name=".MainActivity"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity
            android:name=".FrameSelect"
            android:exported="false" />

        <meta-data
            android:name="preloaded_fonts"
            android:resource="@array/preloaded_fonts" />
    </application>

</manifest>